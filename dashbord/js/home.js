angular.module("myApp").controller("homeCtrl",function ($scope,$rootScope,$state,$timeout,$http) {
    // if(!$rootScope.user){
    //     $state.go('login')
    // }

    wow = new WOW(
        {

        }	)
        .init();
})
